//  Check Uppercase: Write an `if-else` statement to check if a character is uppercase. Print "Uppercase" 
// if true, otherwise "Lowercase."


let char = "A";

if (char === char.toUpperCase()) {
    console.log("Uppercase");
} else {
    console.log("Lowercase");
}